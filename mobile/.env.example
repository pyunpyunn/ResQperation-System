# Optional. Leave this blank for normal Expo Go LAN testing.
# The app automatically uses the same laptop IP that Expo is running from.
#
# If your phone cannot reach the backend, copy this file to .env and set:
# EXPO_PUBLIC_API_BASE_URL=http://YOUR_LAPTOP_WIFI_IP:8000/api
